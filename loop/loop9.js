// 9. Faça um programa que receba um número do usuário e mostre no console a contagem regressiva até 0.

let num = parseInt(prompt("Digite o primeiro número para contagem regressiva"))

while (num >= 0) {
    console.log(num)

    num --
    
}